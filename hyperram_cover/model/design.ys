# running in hyperram_cover/src/
read -formal hyperram.v
prep -top hyperram

memory_nordff
clk2fflogic
chformal -live -fair -remove
opt_clean
setundef -anyseq
opt -keepdc -fast
check
hierarchy -simcheck
write_ilang ../model/design.il
